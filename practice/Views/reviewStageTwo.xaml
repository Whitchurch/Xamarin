<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="practice.Views.reviewStageTwo">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout>
                <Label Text="Review Stage 2" TextColor="Red"></Label>
                <Label Text="Let us implement: 1)Phone Database using SQL-Lite(ORM) approach." HorizontalOptions="Center" VerticalOptions="CenterAndExpand" TextColor="DarkGreen" />
                <Label Text="That will complete my Second revision- Whitchurch" HorizontalOptions="Center" VerticalOptions="CenterAndExpand" TextColor="DarkGoldenrod" />
                <Button Text="Next Stage" BackgroundColor="Green" TextColor="White" Command="{Binding NextCommand}"></Button>
                <Button Text="Return to Page" BackgroundColor="Red" TextColor="White" Command="{Binding BackCommand}"></Button>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>
